using AutoMapper;
using Hipicapp.Maps.Authentication;

namespace Hipicapp.Maps
{
    public sealed class MapperStarter
    {
        public static void Startup()
        {
            /*Mapper.Initialize(cfg =>
            {
                cfg.AddProfile<UserProfile>();
            });*/
        }
    }
}